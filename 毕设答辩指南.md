# 智学引擎——基于 RAG 的智能学习辅助系统

## 答辩指南

> 本文档帮助你从零理解整个项目，答辩时对每个模块了如指掌。
> 读完后你应该能回答所有答辩问题。

---

## 一、项目是什么（一句话）

**智学引擎是一个基于 RAG（检索增强生成）技术的智能学习辅助系统。**
用户上传学习资料（PDF/Word/PPT等），系统自动解析、切分、建立向量索引，
用户提问时，系统从资料中检索最相关的片段，交给大语言模型生成精准回答。

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端 (React + TypeScript)             │
│  ┌──────────┬──────────┬──────────┬──────────┬────────────┐ │
│  │ 登录注册 │ 智能问答 │ 资料管理 │ PDF阅读器│ 管理后台   │ │
│  └──────────┴──────────┴──────────┴──────────┴────────────┘ │
│                          ↕ HTTP / SSE                       │
├─────────────────────────────────────────────────────────────┤
│                     后端 (Spring Boot 3.5)                   │
│  ┌──────────┬──────────┬──────────┬──────────┬────────────┐ │
│  │ 认证模块 │ RAG模块  │ 资料模块 │ LLM模块  │ 管理模块   │ │
│  └──────────┴──────────┴──────────┴──────────┴────────────┘ │
│         ↕              ↕              ↕              ↕      │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐  │
│  │  MySQL   │  │  Redis   │  │  Qdrant  │  │  LLM API   │  │
│  │ 业务数据 │  │ 缓存限流 │  │ 向量存储 │  │ OpenAI等   │  │
│  └──────────┘  └──────────┘  └──────────┘  └────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 三层架构

```
Controller（接收请求）→ Service（处理业务）→ Repository（操作数据库）
```

### 请求生命周期

```
用户点击按钮
    ↓
前端发 HTTP 请求（带 Token）
    ↓
Nginx 反向代理
    ↓
AuthInterceptor 拦截器（验证 Token，提取 userId）
    ↓
Controller（接收参数，调用 Service）
    ↓
Service（业务逻辑：检索、LLM调用等）
    ↓
Repository（生成 SQL，操作 MySQL）
    ↓
返回 ApiResponse{code, message, data}
    ↓
前端渲染结果
```

---

## 三、技术栈

### 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| Java | 21 | 编程语言 |
| Spring Boot | 3.5.6 | Web框架 |
| Spring Data JPA | — | 数据库ORM |
| MySQL | 8.0 | 业务数据库 |
| Redis | 7 | 缓存、验证码、限流 |
| Qdrant | 1.12 | 向量数据库 |
| Flyway | — | 数据库版本管理 |
| PDFBox | 3.0.5 | PDF文本提取 |
| Apache POI | 5.3.0 | Word文本提取 |
| jjwt | 0.12.6 | JWT Token生成/解析 |
| Docker | — | 容器化部署 |

### 前端

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.3 | UI框架 |
| TypeScript | 5.7 | 类型安全 |
| Vite | 6.0 | 构建工具 |
| Tailwind CSS | 3.4 | 样式 |
| shadcn/ui | — | UI组件库（Radix UI） |
| TanStack Query | 5.6 | 数据请求缓存 |
| Axios | 1.7 | HTTP客户端 |
| React Router | 6.28 | 路由 |
| PDF.js | 4.10 | PDF阅读器 |
| Framer Motion | 11.15 | 动画 |

### 部署

| 技术 | 用途 |
|------|------|
| Docker Compose | 一键编排5个容器 |
| Nginx | 反向代理 + 静态资源托管 |
| Let's Encrypt | 免费HTTPS证书 |

---

## 四、核心功能模块

### 模块1：认证系统

**做什么**：用户注册、登录、Token验证、权限控制

**流程**：
```
注册：用户提交用户名+密码 → BCrypt加密密码 → 存入数据库 → 返回Token
登录：用户提交用户名+密码 → 查数据库 → BCrypt验证密码 → 生成JWT Token返回
请求：前端每次请求带 Authorization: Bearer <token> → 拦截器解析Token → 提取userId
```

**关键代码**：
- `AuthInterceptor.java:94` — 拦截器，每个请求都要过这里
- `TokenService.java` — JWT Token的生成和解析
- `PasswordHasher.java` — BCrypt密码加密

**答辩要点**：
- 密码用BCrypt加密存储，不存明文
- Token有版本号机制，改密码后旧Token立即失效
- 拦截器自动注入userId，Controller不需要手动解析Token

---

### 模块2：学习资料管理

**做什么**：上传文件 → 解析文本 → 切分片段 → 生成向量 → 建立索引

**流程**：
```
用户上传PDF
    ↓
后端保存文件到磁盘
    ↓
PDFBox提取文本（逐页）
    ↓
文本分块（300-800字/块，120字重叠）
    ↓
Embedding API生成向量（每块一个向量）
    ↓
存入MySQL（片段文本+元数据）
存入Qdrant（向量索引）
```

**关键设计**：
- **分片上传**：大文件（>1MB）切成1MB一片，逐片上传，支持断点续传
- **异步解析**：PDF解析在后台线程执行，不阻塞用户
- **多格式支持**：PDF、Word、PPT、TXT、Markdown、HTML、网页URL
- **OCR支持**：扫描版PDF用Tesseract识别文字

**答辩要点**：
- 为什么分块？因为LLM有上下文长度限制，而且小片段检索更精准
- 为什么重叠？因为相邻片段可能在语义上相关，重叠保证不丢失上下文
- 分块大小300-800字是经验值，太小丢失上下文，太大检索不精准

---

### 模块3：RAG问答（核心）

**做什么**：用户提问 → 检索相关资料片段 → 交给LLM生成回答

**完整流程**：

```
用户提问: "什么是快速排序？"
    │
    ▼
① 查询理解
    ├─ 识别问题类型（定义类/对比类/概览类）
    ├─ 提取关键词（"快速排序"）
    └─ 判断是否需要检索（通用聊天不检索）
    │
    ▼
② 多路检索
    ├─ 向量检索：把问题转成向量，在Qdrant中找最相似的片段
    ├─ BM25检索：用关键词在MySQL中做全文匹配
    ├─ 摘要种子：如果资料摘要包含关键词，补充前几个片段
    └─ HyDE：让LLM生成假设性回答，用回答的向量去检索
    │
    ▼
③ 混合融合
    ├─ 各路结果按权重融合：向量×0.48 + BM25×0.30 + 摘要×0.12 + 关键词×0.10
    └─ 分数归一化后加权求和
    │
    ▼
④ Reranker精排
    └─ 用Cross-Encoder模型对融合结果重新打分排序
    │
    ▼
⑤ 上下文构建
    ├─ 选出Top-N片段，格式化为上下文文本
    ├─ 加入对话历史（最近8-16轮）
    ├─ 加入长期记忆摘要
    └─ 总上下文控制在14000字符以内
    │
    ▼
⑥ LLM生成
    ├─ 构造Prompt：系统提示词 + 上下文 + 用户问题
    ├─ 调用LLM API（支持流式输出）
    └─ 支持多模态（图片+文本）
    │
    ▼
⑦ 后处理
    ├─ 装饰回答（添加引用来源标注）
    ├─ 保存问答记录到数据库
    ├─ 更新会话长期记忆
    └─ 记录Token消耗
    │
    ▼
返回给前端：回答文本 + 引用来源列表
```

**核心算法详解**：

#### BM25算法
```
BM25是最经典的关键词检索算法，通过统计词频(TF)和逆文档频率(IDF)计算相关性：

IDF(词) = ln((总文档数 - 包含该词的文档数 + 0.5) / (包含该词的文档数 + 0.5) + 1.0)
TF归一化 = (词频 × (k1+1)) / (词频 + k1 × (1-b+b×文档长度/平均文档长度))

其中 k1=1.5, b=0.75 是经验值。
IDF越高说明这个词越稀有（区分度越高），TF越高说明这个词在当前文档中出现越多。
```

#### 向量检索
```
1. 把文档片段通过Embedding API转成向量（如1536维的浮点数数组）
2. 把用户问题也转成同样维度的向量
3. 计算问题向量和所有片段向量的余弦相似度
4. 相似度最高的Top-K个片段就是检索结果
```

#### 混合检索的优势
```
只用向量：语义相近但关键词不匹配时效果好（如"排序算法"能匹配"快排"）
只用BM25：关键词精确匹配效果好（如"时间复杂度O(nlogn)"能精确命中）
混合：两者互补，覆盖更多场景
```

#### HyDE（假设性文档嵌入）
```
问题: "什么是快排"
    ↓ LLM生成假设性回答
假设回答: "快速排序是一种分治排序算法，通过选择基准元素将数组分为两部分..."
    ↓ 用这个回答的向量去检索
找到真正相关的文档片段

原理：回答的语义比问题更接近文档内容，所以检索更准
```

**答辩要点**：
- 为什么用混合检索而不是只用向量？因为两者互补，向量擅长语义，BM25擅长精确匹配
- HyDE的原理是什么？先让LLM假装回答，用回答的向量检索，因为回答的语义更接近文档
- 上下文长度怎么控制？片段14000字符 + 历史12000字符 + 记忆5000字符，总计不超过模型限制

---

### 模块4：流式输出（SSE）

**做什么**：AI回答时逐字输出，而不是等全部生成完再返回

**原理**：
```
传统方式：用户提问 → 等10秒 → 一次性返回完整回答
流式方式：用户提问 → 1秒开始看到字 → 边生成边显示
```

**技术实现**：
```
前端：用 fetch API 读取 ReadableStream，逐帧解析 SSE 事件
后端：用 StreamingResponseBody + SSE 协议，每生成一段就推送给前端

SSE事件格式：
event: status        → {"stage":"searching"}     // 正在检索
event: chunk         → {"delta":"快速"}           // 文本增量
event: chunk         → {"delta":"排序"}           // 文本增量
event: sources       → {"sources":[...]}          // 引用来源
event: done          → {"questionId":123}         // 完成
```

**答辩要点**：
- 为什么用SSE不用WebSocket？SSE更简单，单向推送够用，不需要双向通信
- 流式输出的好处？用户体验好，不用等10秒才看到回答
- 前端怎么处理？用fetch的ReadableStream逐帧读取，解析SSE格式

---

### 模块5：PDF阅读器

**做什么**：在浏览器中直接阅读PDF，支持翻页、搜索、边读边问

**技术**：
- 用 PDF.js 渲染PDF页面
- 后端预先将PDF每页渲染为PNG图片
- 前端按需加载页面图片
- 支持文本层覆盖（在图片上叠加透明文本，支持选中复制）
- 支持"边读边问"：用户在阅读时可以针对当前页面提问

---

### 模块6：管理后台

**做什么**：管理员查看系统数据、管理用户、管理资料

**功能**：
- 仪表盘：用户数、资料数、问答数等统计
- 用户管理：查看列表、修改角色、禁用账号
- 资料管理：查看所有用户上传的资料
- 使用记录：Token消耗统计
- 系统日志：管理员操作审计
- 系统自检：检查PDF工具、OCR、Qdrant等依赖是否可用

---

## 五、数据库设计

### 核心表

```
sys_user                → 用户表（用户名、密码哈希、角色、状态）
learning_material       → 学习资料表（标题、文件路径、解析状态）
material_chunk          → 知识片段表（文本、向量、关键词、页码）
rag_question            → 问答记录表（问题、回答、模型、Token消耗）
rag_question_source     → 问答来源表（引用了哪些片段）
rag_feedback            → 用户反馈表（点赞/踩）
rag_conversation_memory → 会话记忆表（长期对话摘要）
user_favorite           → 收藏表
material_summary        → 资料摘要表
usage_record            → 使用记录表（Token消耗统计）
system_log              → 系统日志表（管理员操作审计）
user_llm_config         → 用户自定义模型配置表
material_page           → 资料页面表（PDF每页信息）
material_processing_job → 处理任务表（后台异步任务）
```

### 表关系

```
sys_user 1:N learning_material（一个用户有多份资料）
learning_material 1:N material_chunk（一份资料有多个片段）
rag_question N:1 sys_user（一个用户有多条问答）
rag_question 1:N rag_question_source（一条问答引用多个片段）
rag_question 1:1 rag_feedback（一条问答最多一条反馈）
```

### 迁移管理

数据库用 Flyway 管理，19个版本的迁移脚本（V1-V19）：
```
V1: 基础表（用户、资料、片段、问答等）
V2: 解析进度字段
V3: 用户LLM配置表
V5: 使用记录表
V10: Token版本号（安全加固）
V14: 会话记忆表
V18: 处理流水线（页面、任务、OCR状态）
V19: 临时资料上下文表
```

---

## 六、前端架构

### 路由结构

```
/                     → 落地页
/login                → 登录
/register             → 注册
/forgot-password      → 忘记密码
/workspace/chat       → 智能问答（核心）
/workspace/materials  → 资料管理
/workspace/reader     → PDF阅读器
/workspace/history    → 问答历史
/workspace/favorites  → 收藏夹
/workspace/summary    → 资料摘要
/admin/dashboard      → 管理仪表盘
/admin/users          → 用户管理
/admin/materials      → 资料管理（管理员）
/admin/evaluation     → RAG评估
/admin/usage-records  → 使用记录
/admin/logs           → 系统日志
```

### 状态管理

```
React Context（认证状态）
    ↓
localStorage（Token持久化）
    ↓
TanStack Query（服务端数据缓存）
    ↓
axios拦截器（自动带Token、自动解包响应、401跳转登录）
```

### 关键设计

1. **懒加载**：所有页面组件用 `React.lazy` + `Suspense`，首屏只加载当前页面
2. **Token管理**：登录后Token存localStorage，每次请求自动带Authorization头
3. **401处理**：后端返回401时自动清除Token并跳转登录页
4. **流式聊天**：用fetch + ReadableStream读取SSE，不用Axios（Axios不支持流式）

---

## 七、部署方案

### Docker Compose 编排

```yaml
services:
  mysql:      # 数据库
  redis:      # 缓存
  qdrant:     # 向量数据库
  backend:    # Java后端
  frontend:   # React前端（Nginx托管）
```

### 一键部署

```bash
docker compose up -d --build
```

### 架构

```
用户浏览器
    ↓
Nginx（端口80/443）
    ├── /           → 前端静态文件
    └── /api/*      → 反向代理到后端:8080
         ↓
Spring Boot后端:8080
    ├── MySQL:3306
    ├── Redis:6379
    ├── Qdrant:6333
    └── LLM API（外网）
```

---

## 八、常见答辩问题及答案

### 基础问题

**Q: 这个项目是做什么的？**
A: 基于RAG技术的智能学习辅助系统。用户上传学习资料，系统自动解析建索引，用户提问时从资料中检索相关片段，交给大模型生成精准回答。

**Q: 什么是RAG？**
A: RAG（Retrieval-Augmented Generation，检索增强生成）是一种结合信息检索和文本生成的技术。先从知识库中检索相关信息，再把检索结果作为上下文交给大模型生成回答。解决了大模型"幻觉"（编造信息）的问题。

**Q: 为什么用RAG而不是直接问大模型？**
A: 大模型的知识截止到训练时间，而且可能编造信息。RAG通过检索真实资料作为依据，让回答更准确、可溯源。

### 技术问题

**Q: 向量检索的原理是什么？**
A: 把文本通过Embedding模型转成高维向量（如1536维浮点数），向量的几何距离反映语义相似度。检索时计算问题向量和所有片段向量的余弦相似度，返回最相似的Top-K个片段。

**Q: 为什么用混合检索而不是只用向量？**
A: 向量检索擅长语义匹配（"排序算法"能匹配"快排"），但对精确关键词匹配较弱。BM25擅长精确匹配（"O(nlogn)"能精确命中），但不理解语义。两者互补，融合后效果更好。

**Q: BM25和向量检索有什么区别？**
A: BM25基于词频统计，是传统的关键词匹配方法，速度快但不理解语义。向量检索基于深度学习的语义理解，能处理同义词和近义词，但需要额外的Embedding计算。

**Q: 什么是HyDE？**
A: HyDE（Hypothetical Document Embeddings）是一种检索优化策略：先让LLM根据问题生成一个"假设性回答"，再用这个回答的向量去检索。因为回答的语义通常比问题更接近文档内容，所以检索更准。

**Q: 文本分块为什么需要重叠？**
A: 因为语义边界不一定和分块边界对齐。重叠120字符可以保证跨块的内容不被切断，比如一个定义可能跨越两个块，重叠后两个块都能检索到。

**Q: SSE和WebSocket有什么区别？**
A: SSE是单向的（服务器→浏览器），基于HTTP，更简单。WebSocket是双向的。流式输出只需要服务器推送，不需要浏览器发消息，所以SSE更合适。

**Q: JWT Token的原理是什么？**
A: JWT由三部分组成：Header（算法）、Payload（用户信息）、Signature（签名）。服务器用密钥签名，客户端存储Token，每次请求带上，服务器验证签名即可确认身份，不需要查数据库。

### 架构问题

**Q: 为什么用Spring Boot而不是其他框架？**
A: Spring Boot是Java生态最成熟的Web框架，自动配置减少样板代码，JPA简化数据库操作，社区活跃文档完善。

**Q: 为什么用MySQL而不是MongoDB？**
A: 项目的数据结构是关系型的（用户-资料-片段-问答有明确的一对多关系），MySQL更适合。向量数据单独存Qdrant，各司其职。

**Q: Redis在项目中做什么？**
A: 三个用途：① 存储验证码（带过期时间） ② 接口限流计数器 ③ 可选的短期状态存储（替代内存实现，支持多实例）。

**Q: Docker Compose的好处是什么？**
A: 一条命令启动所有服务（MySQL、Redis、Qdrant、后端、前端），环境一致，部署简单，适合中小规模应用。

### 创新性问题

**Q: 这个项目的创新点是什么？**
A: 三个创新点：
1. **多路混合检索融合**：向量+BM25+摘要种子+关键词覆盖率四路融合，权重经过实验调优
2. **HyDE检索策略**：用LLM生成假设性回答辅助检索，提高召回率
3. **自适应检索策略**：根据问题类型（定义类/概览类/精确查询）自动选择不同的检索方式

**Q: 和现有的AI问答产品（如ChatGPT）有什么区别？**
A: ChatGPT基于预训练知识回答，可能编造信息。本系统基于用户上传的真实资料回答，每个回答都有引用来源，可溯源验证。

---

## 九、关键代码位置速查（精确到行号）

> 答辩时老师问"这段代码在哪里"，直接翻到对应行号。

### 后端核心代码

| 功能 | 文件路径 | 行号 | 方法名 |
|------|---------|------|--------|
| **项目启动入口** | `LearningAssistantApplication.java` | :20 | `main()` |
| **拦截器（Token验证）** | `auth/AuthInterceptor.java` | :94 | `preHandle()` |
| **用户注册** | `auth/AuthService.java` | :92 | `register()` |
| **用户登录** | `auth/AuthService.java` | :222 | `login()` |
| **密码加密** | `auth/PasswordHasher.java` | :36 | `hash()` |
| **Token生成** | `auth/TokenService.java` | :35 | `generateToken()` |
| **Token解析** | `auth/TokenService.java` | :50 | `parseClaims()` |
| **资料上传** | `material/MaterialService.java` | :510 | `upload()` |
| **PDF文本提取** | `material/MaterialService.java` | — | `extractPdfText()` |
| **文本分块** | `material/MaterialService.java` | — | `splitTextIntoChunks()` |
| **生成向量** | `material/MaterialService.java` | — | `generateEmbeddings()` |
| **写入向量库** | `material/MaterialService.java` | — | `indexToVectorStore()` |
| ****RAG问答入口（核心）** | `rag/RagService.java` | **:421** | `chat()` |
| **检索调度** | `rag/RagService.java` | **:1646** | `selectContextChunks()` |
| **多路混合检索** | `rag/RagService.java` | **:1726** | `selectVectorOrKeywordChunks()` |
| **查询扩展** | `rag/RagService.java` | **:1816** | `expandedRetrievalQueries()` |
| **HyDE检索** | `rag/RagService.java` | **:1843** | `hydeRetrievalQuery()` |
| **摘要种子检索** | `rag/RagService.java` | **:1774** | `findSummarySeedChunks()` |
| **混合融合+权重** | `rag/RagService.java` | **:1990** | `fuseAndRerankChunks()` |
| **融合权重公式** | `rag/RagService.java` | **:2038** | `0.48*vector+0.30*bm25+0.12*summary+0.10*term` |
| **Reranker精排** | `rag/RagService.java` | **:2059** | `rerankChunks()` |
| **上下文构建** | `rag/RagService.java` | **:2694** | `buildExcerpts()` |
| **超长文档分段** | `rag/RagService.java` | **:939** | `longDocumentPlan()` |
| **用户反馈（点赞/踩）** | `rag/RagService.java` | **:3532** | `submitFeedback()` |
| **RAG评估** | `rag/RagService.java` | **:3567** | `evaluateHistory()` |
| **资料摘要生成** | `rag/RagService.java` | **:3936** | `summarize()` |
| **流式问答入口** | `rag/RagStreamController.java` | **:107** | `chatStream()` |
| **SSE事件发送** | `rag/RagStreamController.java` | **:183** | `sendEvent()` |
| **SSE文本分块推送** | `rag/RagStreamController.java` | **:221** | `sendChunkEvents()` |
| **BM25打分** | `rag/Bm25Scorer.java` | **:125** | `score()` |
| **BM25分词** | `rag/Bm25Scorer.java` | **:168** | `tokenize()` |
| **接口限流** | `security/RateLimitService.java` | **:160** | `check()` |
| **SSRF防护** | `security/OutboundUrlGuard.java` | **:80** | `requirePublicHttpUrl()` |
| **全局异常处理** | `common/GlobalExceptionHandler.java` | **:62** | `handleBusiness()` |

### 前端核心代码

| 功能 | 文件路径 | 关键位置 |
|------|---------|---------|
| **路由配置** | `routes/index.tsx` | `router` 定义所有页面路由 |
| **认证状态管理** | `context/AuthContext.tsx` | :84 `AuthProvider` — 登录/登出/Token管理 |
| **HTTP请求封装** | `lib/axios.ts` | :24 请求拦截器（自动带Token） |
| **401处理** | `lib/axios.ts` | :71 响应拦截器（Token过期跳登录页） |
| **SSE流式聊天** | `api/rag.ts` | :88 `chatStream()` — fetch+ReadableStream读SSE |
| **SSE帧解析** | `api/rag.ts` | :114 `processFrame()` — 解析event+data |
| **分片上传** | `api/materials.ts` | :235 `uploadMaterialInChunks()` — 1MB分片+重试 |
| **上传进度轮询** | `api/materials.ts` | :311 `waitForUploadProcessing()` — 1.5秒轮询 |

### 关键配置文件

| 文件 | 路径 | 用途 |
|------|------|------|
| **后端配置** | `backend/src/main/resources/application.yml` | 数据库、Redis、LLM、OCR等所有配置 |
| **融合权重** | `application.yml` → `app.reranker` | 检索融合权重配置 |
| **数据库迁移** | `backend/src/main/resources/db/migration/V1-V19` | 19个版本的SQL迁移脚本 |
| **Docker编排** | `docker-compose.yml` | 5个容器的编排配置 |
| **前端环境** | `frontend/.env.example` | API地址等环境变量 |

### 融合权重代码（答辩重点）

```java
// RagService.java:2038 — 这是整个RAG最核心的一行
double fusedScore = (0.48 * vectorScore)    // 向量检索权重 48%
    + (0.30 * bm25Score)                    // BM25关键词检索权重 30%
    + (0.12 * summaryScore)                 // 摘要种子检索权重 12%
    + (0.10 * termScore);                   // 关键词覆盖率权重 10%
```

### RAG完整流程代码对应

```
用户提问
    │
    ▼
① RagService.chat()                         ← :421  问答入口
    │
    ▼
② selectContextChunks()                      ← :1646 检索调度
    ├─ selectVectorOrKeywordChunks()          ← :1726 多路混合检索
    │   ├─ expandedRetrievalQueries()         ← :1816 查询扩展
    │   ├─ findVectorScoredChunks()           ← 向量检索
    │   ├─ findScoredChunks()                ← BM25检索
    │   ├─ hydeRetrievalQuery()               ← :1843 HyDE检索
    │   └─ findSummarySeedChunks()            ← :1774 摘要种子检索
    ├─ fuseAndRerankChunks()                  ← :1990 融合+重排
    │   └─ 权重公式: 0.48*v + 0.30*b + 0.12*s + 0.10*t  ← :2038
    └─ rerankChunks()                         ← :2059 Reranker精排
    │
    ▼
③ buildExcerpts()                            ← :2694 上下文构建
    │
    ▼
④ LLM生成（调用ThirdPartyLlmClient）
    │
    ▼
⑤ 后处理：保存记录、更新记忆、记录Token消耗
    │
    ▼
⑥ 返回 RagChatResponse
```

---

## 十、项目数据

### 代码规模

| 指标 | 数量 |
|------|------|
| Java文件数 | ~180个 |
| 前端组件数 | ~70个 |
| 数据库表 | 14张 |
| 数据库迁移 | 19个版本 |
| API接口数 | ~50个 |

### 当前部署状态

```
服务器IP: 122.152.235.253
前端: http://122.152.235.253
后端: http://122.152.235.253/api/health → {"status":"ok","service":"智学引擎"}
```

---

> **答辩前必读**：通读本文档，重点看第四节（核心功能模块）和第八节（常见问题）。
> 能画出系统架构图和RAG流程图，基本就稳了。
