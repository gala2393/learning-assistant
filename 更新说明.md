# 更新说明

## v1.3.0 - 2026-06-11

本次版本目标是把项目从“核心功能可跑”推进到“可交付、可部署、可验收”的状态，重点修复资料上传解析、图片型 PDF、临时资料多轮问答、暂停输出、Qdrant 部署和服务器上线流程。

### 资料上传与解析

- 修复删除资料后再次上传同一文件被旧上传会话误判为成功的问题，确保会重新发起上传和解析。
- 大 PDF 上传后先入库，再按批次推进文本提取、OCR 和索引任务，避免 200MB 以上 PDF 长时间卡在 `0%`。
- 图片型 PDF 支持分批 OCR，避免一次性处理数百页导致任务阻塞。
- 资料卡片展示上传、文本、OCR、索引等阶段状态，让用户能看见当前解析进度。
- TXT、Word、大 PDF 解析失败时尽量记录明确失败原因，避免无限停留在 `85%` 或“渲染中”。
- 删除解析中资料时补充任务取消和状态反馈，减少 MySQL 锁等待超时风险。

### 阅读器与资料预览

- PDF 阅读器支持页面预览与 OCR 状态提示，OCR 未完成时仍可查看已生成页面。
- 修复图片型 PDF 只显示第一页、后续页一直渲染的问题。
- 优化 TXT 和 Word 阅读器基础排版，优先保证正文可读。
- 边读边问会保存当前资料上下文，切换到其他模块再回来时尽量恢复原资料、页码和问答状态。
- 已绑定资料状态在阅读器右侧问答区域保持同步，减少“明明打开资料但返回后变成未选择资料”的问题。

### 智慧问答

- 普通智能问答支持临时资料跨轮上下文，不再只对一轮回答有效。
- 已发送过的临时资料不会反复出现在输入框上方，避免用户误以为资料仍待发送。
- 临时资料内容读取失败时会返回更明确的提示。
- 提高问题和上下文输入上限，降低长文本直接返回错误的概率。
- 流式回答支持暂停输出，用户可以中断过长回答。
- 长回答和模型异常增加错误处理，减少前端无限等待。

### Qdrant 与 RAG

- 新增 Qdrant Docker 服务，生产环境使用 `learning_qdrant_data` volume 保存向量库数据。
- 后端支持 Qdrant 健康检查，管理员后台可以看到向量库依赖状态。
- 后台资料管理增加“重建向量索引”入口，历史资料不需要重新解析即可补建向量索引。
- 默认关闭 HyDE 扩展，减少额外模型调用带来的延迟。
- 保留本地 rerank fallback，向量服务不可用时尽量保证问答链路仍可用。

### 管理后台

- 管理员资料列表补充解析状态、索引状态和重建向量索引操作。
- 后台依赖检查从静态配置判断改为真实访问服务健康接口。
- 补全部分管理员路由和权限数据，使后台菜单与权限策略更一致。

### 部署与运维

- 生产部署改为 Docker named volume：
  - `learning_mysql_data`
  - `learning_redis_data`
  - `learning_app_data`
  - `learning_qdrant_data`
- 新增 `DOCKER_DEPLOY.md`，记录公开版 Docker 部署、Qdrant 和数据卷注意事项。
- 新增 `deploy/server.env.example`，说明 Docker 部署所需环境变量。
- 前端新增 Dockerfile 和 Nginx 配置，支持后续完整容器化构建。
- 提醒生产环境不要执行 `docker compose down -v`，避免删除 volume 数据。

### 本次线上验证

- 生产环境 `/api/health` 返回 `200`
- Qdrant `/healthz` 检查通过
- Flyway 已升级到 `v19`

### 已知后续工作

- 历史资料启用 Qdrant 后需要在后台触发“重建向量索引”，否则旧资料不会自动进入向量库。
- Qdrant 能提升 RAG 检索阶段速度，但不能加速大模型本身的输出速度。
- 图片型 PDF 目前优先保证“可分批 OCR + 状态可见”，复杂版面还原仍需后续增强。
- TXT/Word 阅读器当前以基础可读为目标，目录、批注、复杂排版仍可继续优化。
- 后续仍需继续做完整浏览器验收，覆盖登录、上传、资料查看、继续阅读、普通问答、资料问答、临时资料多轮、暂停输出、删除重传和后台管理。

## 2026-06-10

本次更新重点修复资料管理和边问边读模块，提升 PDF、Word、TXT 等资料上传稳定性，并升级阅读器的 PDF 页面展示、页码切换和划词上下文能力。

### 功能更新

- 停用 MinerU 主链路，PDF 默认走内置解析器和 Poppler 快速导入，避免真实大 PDF 上传时被外部解析进程长时间阻塞。
- 新增资料页面文本块存储，用页面文本层支撑 PDF 阅读器划词和上下文读取。
- 优化 PDF 上传：持久资料支持更大的文件体积，普通 PDF 优先识别原生文本层，扫描件可依赖 OCR 兜底生成可检索、可问答文本。
- 修复 `.doc` 等旧版 Word 文件上传兼容问题，配合 LibreOffice 文档转换能力生成可预览内容。
- 修复 TXT 和大资料上传过程中进度停在向量写入阶段的问题：资料片段先保存入库，向量索引改为后台补齐。
- 阅读器改为更接近 A4 纸张的页面预览体验，并修复页码输入时被滚动监听覆盖的问题。
- PDF/页面预览模式底部操作改为“上一页 / 下一页”，文本片段模式保留“上一片段 / 下一片段”，减少阅读时的认知混淆。
- 调整资料问答和边问边读绑定提示位置，已绑定资料显示在问答框上方，减少占用阅读区空间。

### 文档更新

- 更新 `README.md` 的最近更新、运行环境、配置说明和 GitHub 提交说明。
- 补充 OCR、LibreOffice、分片上传和异步向量索引说明。
- 更新开发教程中的数据库设计、核心功能实现和部署说明。

### 仓库清理

- 不上传本地部署包、服务器私钥、登录 payload、测试脚本、前端构建产物和后端打包产物。
- 代码仓库只提交源码、配置样例、数据库迁移脚本、测试代码、README、开发教程和发布说明。

### 验证

- 前端构建：`npm.cmd run build`
- 后端编译：`mvnw.cmd -q -DskipTests compile`
- 后端打包：`mvnw.cmd -q -DskipTests clean package`
- 服务器健康检查：`/api/health` 返回 `status=ok`
- 测试账号登录验证通过，并完成 TXT 资料线上上传测试
