# 更新说明

## 2026-06-08

本次更新重点改善智能问答的附件能力、流式回答阅读体验，并清理 GitHub 仓库中的本地运行产物。

### 功能更新

- 优化流式回答滚动体验：用户停留在底部时自动跟随最新回答；用户上滑阅读已生成内容时暂停自动滚动，并显示“回到底部查看最新回答”按钮。
- 完善通用问答临时资料能力：支持临时上传 PDF、Word、PPT、Markdown、TXT 等文件作为当前对话上下文，不进入资料库。
- 补充图片与临时资料历史回显说明：问题附带的图片和临时资料会随问答历史保存，历史会话可继续查看附件卡片与预览。
- 补充前后端关键代码注释，覆盖临时资料、图片历史、对话归档、附件卡片和预览弹窗等流程。

### 文档更新

- 更新 `README.md` 项目概述，加入临时资料问答、流式阅读保护和 GitHub 安全排除说明。
- 更新 `开发教程/01-项目总览.md`、`开发教程/03-数据库设计.md`、`开发教程/04-前端搭建.md`、`开发教程/05-核心功能实现.md`，补充附件、历史回显和临时资料设计。

### 仓库清理

- 不再上传本地部署包、服务器私钥、构建产物、运行截图和测试产物。
- 已通过 `.gitignore` 排除 `.codex-run/`、`output/`、`test-results/`、`upload-package/`、`upload-package-latest/`、`frontend-dist-upload/`、`frontend/dist/`、`*.ppk`、`*.key`、`*.zip` 和压缩包产物。
- 代码仓库只保留源码、配置样例、教程文档和必要项目文件，避免暴露管理员密码、数据库密码或大模型密钥。

### 验证

- 前端构建：`npm.cmd run build`
- 后端重点测试：`mvnw.cmd "-Dtest=OpenAiCompatibleLlmClientTest,ThirdPartyLlmClientTest,RagStreamControllerTest" test`
